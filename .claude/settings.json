{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "out=$(uv run prek run --all-files 2>&1); rc=$?; [ $rc -eq 0 ] || { printf '%s\n' \"$out\" >&2; exit 2; }",
            "statusMessage": "Running prek"
          }
        ]
      }
    ]
  }
}
